// Os modules - It gives the information related to operating system

let op = require('os');

console.log(op.freemem());  // it gives the available memory space of the system

console.log(op.hostname()); // it gives the host name 

console.log(op.homedir());

console.log(op.release());

console.log(op.platform());

console.log(op.arch());



